[pytest]
testpaths = tests
markers =
    docker: requires a real, reachable Docker daemon (skipped automatically if none is available)
    network: makes a real network call (e.g. Safety's vulnerability database lookup)
